import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'// in case of App i can Use any name 
//import {App} from './App'
const root= ReactDOM.createRoot(document.getElementById('root'))
root.render(<App/>)
